import 'package:flutter/material.dart';

class AuthLayou extends StatelessWidget {
  const AuthLayou({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Placeholder(),
    );
  }
}